<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConsumoEnergetico"
        tools:targetApi="31">

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ConsumoEnergetico">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para gráficos -->
        <activity
            android:name=".Grafica"
            android:label="@string/title_activity_grafica"
            android:theme="@style/Theme.ConsumoEnergetico" />

        <!-- Actividad para registro manual -->
        <activity
            android:name=".RegistroManual"
            android:label="@string/title_activity_registro_manual"
            android:theme="@style/Theme.ConsumoEnergetico" />

        <!-- Otras actividades pueden ser añadidas aquí si las necesitas -->

    </application>

</manifest>
